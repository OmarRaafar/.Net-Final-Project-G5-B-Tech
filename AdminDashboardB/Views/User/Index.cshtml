@* @model IEnumerable<UserViewModel> *@

@{
    ViewData["Title"] = "Index";
}


<div class="container my-5 shadow p-4 rounded">
    <h2 class="shadow rounded-3 bg-dark mb-5 mt-4 rounded p-1 col-12 col-md-3 text-center text-light fw-bold ">All Users</h2>
    @*
    @if (Model.Count() > 0)
    {
    *@
    <div class="table-responsive shadow rounded-3">
        <table class="table table-hover text-center align-middle">
            <thead class="table-dark shadow">
                <tr>
                    <th>UserName</th>
                    <th>PhoneNumber</th>
                    <th>Email</th>
                    <th>Roles</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @*
                @foreach (var user in Model)
                {
                *@
                <tr>
                    <td>UserName</td>
                    <td>PhoneNumber</td>
                    <td>Email</td>
                    <td>userRole</td>
                    <td>
                        <a class="btn btn-success btn-sm" asp-action="Edit" asp-route-id="Id">
                            <i class="fas fa-edit"></i> Edit
                        </a>
                    </td>
                </tr>
                @*   } *@
            </tbody>
        </table>
    </div>
    @*
    }
    else
    {
    *@
    <div class="alert alert-warning text-center">
        <h2>No Records Found</h2>
    </div>
    @*    } *@

</div>
