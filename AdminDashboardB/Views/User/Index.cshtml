﻿@* @model IEnumerable<UserViewModel> *@

@{
    ViewData["Title"] = "Index";
}


<div class="container my-5 shadow p-4 rounded">

    <div class="row mb-5 mt-4 align-items-center">
        <div class=" shadow rounded-3 bg-dark ms-2  rounded p-1 col-12 col-md-3 text-center text-light fs-5 fw-bold">
            <label for="search" class="form-label ">All Users</label>

        </div>
        <div class="col-12 col-md-5">
            <form class="d-flex" role="search" asp-action="Search" asp-controller="User" method="get">
                <input class="shadow form-control me-2" type="search" name="name" placeholder="Search User" aria-label="Search">
                <button class="shadow btn btn-primary" type="submit">Search</button>
            </form>
        </div>

        <div class="col-12 col-md-3  text-center mt-3 mt-md-0">
            <a asp-action="Create" class="shadow btn btn-success">
                <i class="fa-solid fa-plus me-2"></i>Create new User
            </a>
        </div>
    </div>
    @*
    @if (Model.Count() > 0)
    {
    *@
    <div class="table-responsive shadow rounded-3">
        <table class="table table-hover text-center align-middle">
            <thead class="table-dark shadow">
                <tr>
                    <th>UserName</th>
                    <th>PhoneNumber</th>
                    <th>Email</th>
                    <th>Roles</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>UserName</td>
                    <td>PhoneNumber</td>
                    <td>Email</td>
                    <td>userRole</td>
                    <td>
                        @*
                        @if (item.LockoutEnd == null | item.LockoutEnd < DateTime.Now)
                        {
                        *@
                        <a class="btn btn-success" asp-action="LockUnlock" asp-route-id="Id">
                            <i class="fas fa-lock-open"></i>
                        </a>
                        @*  }
                        else
                        { *@
                        <a class="btn btn-danger" asp-action="LockUnlock" asp-route-id="Id">
                            <i class="fas fa-lock"></i>
                        </a>
                        @*  } *@
                        <a class="btn btn-danger text-white" asp-action="Delete" asp-route-id="Id">
                            <i class="fa-solid fa-trash-can"></i> Delete
                        </a>
                    </td>
                </tr>
                @*  } *@
            </tbody>
        </table>
    </div>
    @*
    }
    else
    {
    *@
    <div class="alert alert-warning text-center">
        <h2>No Records Found</h2>
    </div>
    @*    } *@

</div>